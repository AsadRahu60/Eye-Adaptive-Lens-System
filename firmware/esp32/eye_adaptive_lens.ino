/**
 * Eye Adaptive Lens System — Arduino Sketch Entry Point
 *
 * This file is the Arduino IDE entry point.  All logic lives in main.cpp.
 * When using Arduino IDE:
 *   1. Open this .ino in Arduino IDE 2.x
 *   2. Ensure main.cpp is in the same sketch folder
 *   3. Install required libraries (see main.cpp header for the full list)
 *   4. Select board: "ESP32S3 Dev Module"
 *   5. Flash
 *
 * When using PlatformIO, use platformio.ini instead — this file is ignored.
 */

// Arduino IDE requires setup() and loop() in the .ino file.
// They are defined in main.cpp and linked automatically by the build system.
// Nothing to add here.
